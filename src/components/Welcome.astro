---
import { Image } from "astro:assets";
import { AnimatedThemeToggler } from "./ui/animated-theme-toggler";
import { LightRays } from "./ui/light-rays";
import me from "../assets/me_stand.png";
---

<div class="h-screen w-full rounded-lg border">
    <div
        class="relative z-10 flex h-full items-center justify-center gap-4 px-6 text-center"
    >
        <span
            class="text-xs font-semibold tracking-[0.35em] text-slate-800/60 uppercase dark:text-slate-200/60"
        >
            Ambient glow
        </span>
        <div>
            <h1 class="text-foreground text-4xl font-bold md:text-5xl">
                Hi, I'm Bryan
                <!-- TODO: add underline effect -->
            </h1>
        </div>
        <Image src={me} alt="A bird sitting on a nest of eggs." width="200" />
    </div>
    <LightRays />
</div>
